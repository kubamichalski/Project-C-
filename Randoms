#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

int main()
{
  srand(time(NULL));
  int tab[6] = {};

  for (int i = 0; i < 6; i++)
  {
    tab[i] = (rand() % 100) + 1;
    cout << tab[i] << endl;
  }

  return 0;
}
