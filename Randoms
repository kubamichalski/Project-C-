#include <iostream>
#include <ctime>
#include <cstdlib>

using namespace std;

int main()
{
	srand(time(NULL));
  int tab[10] = {};
  
  for (int i=0; i<10 ; i++ )
  {
  	tab[i] = (rand() % 89) + 10;
    cout << tab[i] << endl;
  }
  
  return 0;
}
